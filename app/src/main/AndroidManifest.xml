<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.darshanudagire.employee">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".sidenavigation.Logout"></activity>
        <activity android:name=".sidenavigation.Share" />
        <activity android:name=".sidenavigation.ReportAnIssue" />
        <activity android:name=".sidenavigation.AboutCompany" />
        <activity android:name=".sidenavigation.MyProfile" />
        <activity android:name=".dashboard.Feedback" />
        <activity android:name=".dashboard.Poll" />
        <activity android:name=".dashboard.Attendence" />
        <activity android:name=".dashboard.AppyForLeave" />
        <activity android:name=".dashboard.Team" />
        <activity android:name=".dashboard.Schedule" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>